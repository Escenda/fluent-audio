# IO Nodes

IO nodes own audio ingress and egress boundaries.

Sources produce explicit audio or device events. Sinks consume them and reject timing, sequence, format, or device failures instead of falling back implicitly.
