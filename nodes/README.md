# Nodes

Executable DORA nodes live here. Leaf directories are process boundaries.

Rust, Python, or mixed-language implementation belongs inside the owning leaf node directory. Shared contracts and helpers belong under `src/fluent_audio`.
