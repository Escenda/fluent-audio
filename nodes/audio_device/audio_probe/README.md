# audio_probe

DORA-only verification sink for audio boundary smoke runs. It validates `audio` input metadata, chunk continuity, total frames/bytes, and the explicit final marker without persisting captured audio.
