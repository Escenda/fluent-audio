# Offline Fixtures

Raw PCM fixtures for local smoke runs belong here.

Unit tests generate their own byte streams in temporary directories so fixture files
do not hide format, channel, or frame-count assumptions.
